import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fldsmdfr.karpiem',
  appName: 'frontend',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
